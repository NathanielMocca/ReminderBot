rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Server SDK with service account bypasses these rules.
    // Keep deny-all for client apps in this architecture.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
